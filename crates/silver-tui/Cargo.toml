[package]
name = "silver-tui"
description = "Terminal user interface for Silver Messenger"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "silver"
path = "src/main.rs"

[dependencies]
silver-protocol = { workspace = true }
silver-client = { workspace = true }
zeroize = { workspace = true }
tokio = { workspace = true }
ratatui = { workspace = true }
unicode-width = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }
clap = { workspace = true }
rpassword = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
qrcode = { workspace = true }
arboard = { workspace = true }
base64 = { workspace = true }
open = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tempfile = "3"

[target.'cfg(unix)'.dependencies]
rlimit = "0.10"

# Desktop notifications (docs/design/notifications.md): the session bus on
# Linux, through the zbus the key store already links; a WinRT toast on
# Windows. macOS uses `osascript` and needs nothing.
[target.'cfg(target_os = "linux")'.dependencies]
nix = { version = "0.29", features = ["process"] }
zbus = { workspace = true }

[target.'cfg(windows)'.dependencies]
tauri-winrt-notification = { workspace = true }
